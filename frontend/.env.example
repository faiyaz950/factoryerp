# Optional: call Laravel directly from the browser (then CORS must allow your Vercel domain).
# If unset, production uses same-origin /api (Vercel server proxy — set LARAVEL_API_URL in Vercel).
# VITE_API_URL=https://your-backend.example.com/api
